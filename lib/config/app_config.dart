class AppConfig {
  // Cloudinary Configuration
  // TODO: In production, load these from environment variables (e.g. flutter_dotenv)
  // or build-time arguments to avoid checking them into version control.
  static const String cloudinaryCloudName = 'dwihjvj2p';
  static const String cloudinaryUploadPreset = 'Quiz-App';
  
  // Feature Flags can also go here
  static const bool enableNewUI = true;
}
